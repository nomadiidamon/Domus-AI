{
  "servers": {
    "filesystem": {
      "command": "python",
      "args": ["-m", "mcp.filesystem"],
      "env": {},
      "transport": "stdio",
      "description": "Read/write access to project files within the host project root."
    },
    "git": {
      "command": "python",
      "args": ["-m", "mcp.git"],
      "env": {},
      "transport": "stdio",
      "description": "Git operations on the host project repository."
    },
    "fetch": {
      "command": "python",
      "args": ["-m", "mcp.fetch"],
      "env": {},
      "transport": "stdio",
      "description": "Fetch and read web content for research tasks."
    }
  }
}
